# ICSA runtime dependencies (pinned to versions verified on Python 3.13).
# Install with:  pip install -r requirements.txt

# Web UI
streamlit==1.59.1
pandas==3.0.3

# LLM + embeddings (Google Gemini)
google-generativeai==0.8.6
python-dotenv==1.2.2

# RAG stack (LangChain + Chroma vector store)
langchain-core==1.4.9
langchain-community==0.4.2
langchain-chroma==1.1.0
langchain-text-splitters==1.1.2
chromadb==1.5.9

# Database (SQLAlchemy ORM + Alembic migrations, SQLite)
SQLAlchemy==2.0.51
alembic==1.18.5

# Auth
bcrypt==5.0.0
PyJWT==2.13.0

# Document parsers (knowledge ingestion)
pypdf==6.14.2
python-docx==1.2.0
